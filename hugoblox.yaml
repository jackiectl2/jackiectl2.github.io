build:
  hugo_version: '0.164.0'  # pinned to the version this site is actually built and tested with
deploy:
  # Deployment target: github-pages, netlify, vercel, cloudflare, or none
  host: 'github-pages'
template:
  id: "academic-cv"
  name: "Academic CV"
